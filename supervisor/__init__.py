"""
Supervisor package.

Orchestrates query classification, sub-agent delegation via parallel dispatch,
and response synthesis. The supervisor discovers sub-agents through the registry
and maintains conversation context across sessions.
"""
