"""
Context package.

Manages per-session conversation history including storage, retrieval,
and summarization. Preserves key entities (legal references, case identifiers,
monetary amounts, party names) during context window truncation.
"""
